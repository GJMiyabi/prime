type User {
  id: ID!
  email: String!
  name: String
  # passwordHash は通常は公開しないので省略
}

input UserSaveInputDto {
  id: ID
  email: String!
  name: String
  password: String!
}

type Mutation {
  saveUser(input: UserSaveInputDto!): User!
}
